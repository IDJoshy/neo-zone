import "./_hero.scss";

const Hero = ({ children }) => {
    return (
        <div className="hero">
            <div className="hero__container">
                <h1 className="hero__title"></h1>
                <p className="hero__subtitle"></p>
            </div>
        </div>
    );
};